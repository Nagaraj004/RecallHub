// DEPRECATED: Dynamic loaders for html2canvas and jsPDF are no longer needed
// RecallHub now uses native browser print (window.print()) with dedicated @media print stylesheets and inline vector SVGs.
export {};
